{{- if .Values.istio.enabled }}
apiVersion: networking.istio.io/v1
kind: VirtualService
metadata:
  name: {{ include "tavo.fullname" . }}
  labels:
    {{- include "tavo.labels" . | nindent 4 }}
spec:
  hosts:
    {{- toYaml .Values.istio.virtualService.hosts | nindent 4 }}
  gateways:
    {{- if .Values.istio.gateway.name }}
    - {{ if .Values.istio.gateway.namespace }}{{ .Values.istio.gateway.namespace }}/{{ end }}{{ .Values.istio.gateway.name }}
    {{- else }}
    - {{ include "tavo.fullname" . }}
    {{- end }}
  http:
    - route:
        - destination:
            host: {{ include "tavo.fullname" . }}
            port:
              number: {{ .Values.service.port }}
{{- end }}
