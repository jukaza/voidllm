{{- $existing := lookup "v1" "Secret" .Release.Namespace (include "tavo.fullname" .) }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "tavo.fullname" . }}
  labels:
    {{- include "tavo.labels" . | nindent 4 }}
type: Opaque
data:
  {{- if .Values.secrets.adminKey }}
  TAVO_ADMIN_KEY: {{ .Values.secrets.adminKey | b64enc | quote }}
  {{- else if and $existing $existing.data (index $existing.data "TAVO_ADMIN_KEY") }}
  TAVO_ADMIN_KEY: {{ index $existing.data "TAVO_ADMIN_KEY" }}
  {{- else }}
  TAVO_ADMIN_KEY: {{ randAlphaNum 48 | b64enc | quote }}
  {{- end }}
  {{- if .Values.secrets.encryptionKey }}
  TAVO_ENCRYPTION_KEY: {{ .Values.secrets.encryptionKey | b64enc | quote }}
  {{- else if and $existing $existing.data (index $existing.data "TAVO_ENCRYPTION_KEY") }}
  TAVO_ENCRYPTION_KEY: {{ index $existing.data "TAVO_ENCRYPTION_KEY" }}
  {{- else }}
  TAVO_ENCRYPTION_KEY: {{ randBytes 32 | b64enc | b64enc | quote }}
  {{- end }}
  {{- if .Values.secrets.license }}
  TAVO_LICENSE: {{ .Values.secrets.license | b64enc | quote }}
  {{- end }}
  {{- range $key, $val := .Values.secrets.extraEnv }}
  {{ $key }}: {{ $val | b64enc | quote }}
  {{- end }}
  {{- if .Values.postgresql.enabled }}
  POSTGRES_PASSWORD: {{ .Values.postgresql.auth.password | b64enc | quote }}
  {{- end }}
